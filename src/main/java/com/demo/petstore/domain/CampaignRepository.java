package com.demo.petstore.domain;

import com.demo.petstore.domain.*;

import org.springframework.data.jpa.repository.JpaRepository;


public interface CampaignRepository extends JpaRepository<Campaign, Long>{    // Repository Pattern Interface
  
}