package com.demo.petstore.domain;

import com.demo.petstore.domain.*;

import org.springframework.data.jpa.repository.JpaRepository;


public interface CampaignDetailRepository extends JpaRepository<CampaignDetail, Long>{    // Repository Pattern Interface
  
}